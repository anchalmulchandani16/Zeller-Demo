import {moderateScale} from '@themes/index';
import {StyleSheet} from 'react-native';

const Styles = StyleSheet.create({
  container: {
    gap: 10,
    marginHorizontal: moderateScale(15),
  },
});

export default Styles;
